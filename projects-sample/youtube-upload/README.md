# youtube-upload sample project

Copy this folder to wherever you want your YouTube project to live, then:

1. `cp upload-defaults.yaml.example upload-defaults.yaml` and edit
2. Drop `.mp4` files into `videos/`
3. `~/.claude/skills/youtube-upload/scripts/upload-queue.sh <this-dir>`

See the main repo README for full details.
